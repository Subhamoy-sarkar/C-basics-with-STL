#include<bits/stdc++.h>
using namespace std; 

int main(){
    // floor(x) : return the largest integer that is smaller than or equal to x (i.e : rounds downs the nearest integer)
    float a=242.234,b=-242.24;
    cout<< floor(a)<< endl<< floor(b)<<endl;

    // ceil(x) : return the smallest integer that is greater than or equal to x (i.e : rounds up the nearest integer)
    float a1=243.24 , b1= -424.24;
    cout<< ceil(a1) << endl << ceil(b) <<endl;
    return 0;
}